FRED_API_KEY=your_fred_api_key_here

# Optional — enables v1.1 FOMC NLP hawk-dove scoring via Claude API.
# Without this, the pipeline runs with static FOMC scores (no API cost).
# Overridable via INFLATION_MONITOR_NLP_MODEL if you want Haiku/Sonnet for cost.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# INFLATION_MONITOR_NLP_MODEL=claude-opus-4-7
